import './Header.css';

const Header = () => {
    return (
    <section className='header'>
        <div>Logo/Menu</div>
        <div>SpotShare</div>
        <div>Profile</div>
    </section>);
};

export default Header;